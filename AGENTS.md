# Backend agent instructions

This is the backend application.

Related frontend workspace root: ../aplikasi-frontend

For full-stack tasks:
- Ask the main agent to delegate frontend work to a frontend subagent.
- Backend subagents may edit only this backend repository.
- Do not edit unrelated workspace folders.
- Document every new or modified API contract.
- Run backend tests before completing the task.